<!DOCTYPE html>
@{
  Layout="Shared/_Layout.cshtml";
}
@{
  string senderName = " ";
}
@if(Model.ContainsKey("message"))
{
  <h3>@Model["message"]</h3>
}

<div class="container">
  <div class="well">
    <h1>From: @Model["sender"].UserName</h1>
  </div>
    <hr>
    <div class="well">
    <h4>@Model["user"].MessageBody</h4>
  </div>

    <button class="btn btn-info"type="button" name="reply"><a href="/user/messages/@Model["sender"].Id/reply">Reply</a></button>
    <button class="btn btn-danger" type="button" name="button">Delete</button>
  </form>
  <p>
    <a href="/profile/@Model["user"].Id">Home</a>
  </p>
</div>
